export default function renderScene(glRenderer, cssRenderer, scene, camera){
    // Needs to create a fresh copy of the scene data to render, without touching our state's copy (ie. no adding children to our state's scene)
    
    //renderer.render(scene, camera)
}